name: Daily Blog Engine

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: daily-blog-engine-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Blog engine is disabled. No posts will be generated or published."
